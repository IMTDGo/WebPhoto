# Dependencies
node_modules/

# Environment variables — NEVER commit this file
.env

# Upload folder contents (keep directory, ignore files)
upload/*
!upload/.gitkeep

# Login log
login_log.txt

#本機的Node.js不用推上去，用Render的
node/